name: 方舟+飞书自动任务
on:
  push:
    branches: ["main"]
  # 支持手动在 Actions 面板触发运行
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # 读取你之前在 Secrets 存入的3组密钥
    env:
      ARK_API_KEY: ${{ secrets.ARK_API_KEY }}
      FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
      FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
    steps:
      # 拉取仓库里你的脚本代码
      - name: 拉取仓库代码
        uses: actions/checkout@v4

      # 举例：如果是Python脚本就启用下面两行
      # - name: 安装Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.11"
      # - name: 运行自动化脚本
      #   run: python main.py
